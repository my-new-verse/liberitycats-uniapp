# 变量必须以 VITE_ 为前缀才能暴露给外部读取
NODE_ENV = 'production'
# 是否去除console 和 debugger
VITE_DELETE_CONSOLE = false

# 测试环境接口配置
VITE_APP_PROXY = false
VITE_SERVER_BASEURL = 'https://test-app.libertycats.app/api'
VITE_IMAGE_HOST = 'https://test-app.libertycats.app/'
